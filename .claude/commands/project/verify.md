Verify that the described work is complete and correct. Provide evidence for every claim.

Steps:
1. Read `.github/copilot-instructions.md` for project conventions.
2. Read the acceptance criteria or task description below.
3. For each criterion, gather evidence (file contents, command output, grep counts).
4. Run quality gates (lint, typecheck, build, tests).
5. Report pass/fail per criterion with evidence.

Rules:
- "It should work" is not evidence. Run commands, read files, prove it.
- Do NOT edit any files. This is a read-only verification.
- If anything fails, list exactly what's missing and what needs to happen.

$ARGUMENTS
