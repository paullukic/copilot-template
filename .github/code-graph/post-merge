#!/bin/sh
# Auto-update code graph after merge operations (including git pull merge mode).
REPO_ROOT=$(git rev-parse --show-toplevel)
cd "$REPO_ROOT" || exit 0
if [ -f ".code-graph/graph.db" ]; then
    python .github/code-graph/server.py --update 2>/dev/null || true
fi
